name: Build iOS IPA

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    name: Build iOS App
    runs-on: macos-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Build and Package IPA
        run: |
          echo "Đang tiến hành build ứng dụng..."
          # Đảm bảo các lệnh build xcodebuild của bạn nằm ở đây
          # Ví dụ: xcodebuild -project YourProject.xcodeproj -scheme "Shinn H4k" ...
